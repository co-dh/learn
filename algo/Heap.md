# A almost complete tree with HEAP PROPERTY: root < children
The most efficient data stracture to implement priority queue.

Almost complete
: only empty node at the bottom. Why? Can be implement in array.

